## Task-12/13/14/15/16: Frontend UI + Verify + Git commit S1

STATUS: DONE
DATE: 2026-05-07

FILES_CREATED:
  - src/components/AddServerDialog.tsx — form + password toggle + test connection + badge
  - src/components/ServerInfoPanel.tsx — side panel: RAM/Disk progress bars, uptime format
  - src/components/ServerInfoPanel.tsx — NOTE: credential not stored in frontend (by design), info fetch placeholder

FILES_MODIFIED:
  - src/pages/Home.tsx — tabs Production/Staging/Lab/All + server table + side panel + AddServerDialog

NOTES:
  - Password field: show/hide toggle (Eye/EyeOff icon) — security rule met
  - Test Connection: spinner → Badge OK (green) / fail (destructive)
  - Delete: confirm() dialog before removeServer()
  - tsc --noEmit: PASS
  - cargo check: PASS (5 unused warnings — expected, dead code until UI calls)
  - 24 files changed, 1666 insertions — committed as feat(s1)

NEXT_TASK: T17 — Server status polling (Sprint S2)
